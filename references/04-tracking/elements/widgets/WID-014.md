---
id: WID-014
kind: widget
widgetDefId: exec-forecast-tools
contextType: Dashboard-local
registry: WIDGETS_EXEC
registrySource: app/src/presentation/pages/Dashboard/widgets/registryExecWidgets.tsx
registryLine: 91
group: 予測・シミュレーション
size: full
linkTo:
  view: insight
  tab: budget
acquisitionPath: ctx-direct
consumedCtxFields:
  - fmtCurrency
  - observationStatus
  - prevYear
  - result
  - storeKey
  - targetRate
consumedReadModels: []
consumedQueryHandlers: []
children:
  - ForecastToolsWidget
lastSourceCommit: f3226c0ac1d7c3f14598e36cce1b545a2725ca13
owner: implementation
specVersion: 1
---

# WID-014 — 着地予測・ゴールシーク

## 1. 概要

`ForecastToolsWidget` に full ctx を passthrough、storeKey を React key として指定する registry entry（registryExecWidgets.tsx:68-74）。

## 2. Data Acquisition Path

- registry 行では `<ForecastToolsWidget key={ctx.storeKey} ctx={ctx} />` の形
- 実際のデータ取得・予測計算は子 `ForecastToolsWidget` 内部に委譲

## 3. Context Fields Consumed — 解説

- `storeKey` は React key 用にのみ直接参照（store 切替で再マウント）
- 実ctx 依存は子側に散在

## 4. Composition

- 直接の子: `ForecastToolsWidget`（`./ForecastTools` から import、:2）
- `ForecastTools.tsx` が単独 file として存在

## 5. Visibility

- `isVisible` 未指定（常時可視）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- 予測計算の不変は子 `ForecastToolsWidget` の `calculateForecast` 等が担当（inquiry/05 関連）

### Behavior Claims (Phase J Evidence Level)

| ID | claim | evidenceLevel | riskLevel | tests | guards | verificationNote |
|---|---|---|---|---|---|---|
| CLM-001 | registry 行は view marshalling 専用 — `consumedCtxFields` のみ参照、raw 計算 / fetch 禁止（A3 描画専用 + C9） | reviewed | high | - | - | 宣言 widget の妥当性は consumedCtxFields 整合 (frontmatterSyncGuard) と R:tag/G8 mechanism (responsibilityTagGuard) で構造的に担保。widget 個別 test は冗長 |
| CLM-002 | visibility predicate で空データ時の dead UI 回避（runtime 空状態防御） | reviewed | medium | - | - | 空状態到達経路は load 中 / fallback / partial 等多岐。widget 個別の predicate test より consumed readModel/calc layer の null 安全契約で構造的保証 |
| CLM-003 | ForecastTools wrap で予測ツール UI 提供、RM-006 calculateFactorDecomposition / CALC-008 calculateForecast 出力を mapping（widget は orchestration のみ、4 層依存遵守） | guarded | high | - | app/src/test/guards/presentationIsolationGuard.test.ts | - |

## 7. Co-Change Impact

- `storeKey: string` 型変更
- `ForecastToolsWidget` の props shape 変更
- `linkTo` の view / tab 型変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`
- `pageMetaGuard`

## 9. Pipeline Concerns / Upstream Requests

- **full ctx passthrough の静的解析不能**: 子 `ForecastToolsWidget` の実 ctx 依存が不可視（WID-008 と同構造）
- **linkTo Dashboard → Insight/budget tab 跨ぎ**: 型 A → 型 B（Insight = Unified）の linkTo + tab 指定。WID-003 / WID-011 / WID-013 と同じ跨ぎパターンの 4 例目
- **`storeKey` React key pattern の 4 例目**: WID-008 / WID-014 / WID-015 / WID-016 の共通 pattern
