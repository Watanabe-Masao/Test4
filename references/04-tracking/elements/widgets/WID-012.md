---
id: WID-012
kind: widget
widgetDefId: exec-daily-inventory
contextType: Dashboard-local
registry: WIDGETS_EXEC
registrySource: app/src/presentation/pages/Dashboard/widgets/registryExecWidgets.tsx
registryLine: 71
group: トレンド分析
size: full
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields: []
consumedReadModels: []
consumedQueryHandlers: []
children: []
lastSourceCommit: f3226c0ac1d7c3f14598e36cce1b545a2725ca13
owner: implementation
specVersion: 1
---

# WID-012 — 日別推定在庫

## 1. 概要

`renderDailyInventoryTable(ctx)` 関数に ctx を委譲する registry entry（registryExecWidgets.tsx:50-55）。

## 2. Data Acquisition Path

- registry 行では `renderDailyInventoryTable(ctx)` を呼び出すのみ

## 3. Context Fields Consumed — 解説

- registry 行からの直接 touch 0
- 実 ctx 依存は `renderDailyInventoryTable` 側
- 在庫推定は `computeEstimatedInventory` 等の domain 関数を helper 内で呼ぶ想定（inquiry/01 §B-5 の `storeDailyLane` permanent floor 関連）

## 4. Composition

- render helper: `renderDailyInventoryTable`（`./TableWidgets` から import、:8）

## 5. Visibility

- `isVisible` 未指定（常時可視）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- 在庫推定の計算不変は `computeEstimatedInventory` 側（inquiry/01 §B-5 記載）

### Behavior Claims (Phase J Evidence Level)

| ID | claim | evidenceLevel | riskLevel | tests | guards | verificationNote |
|---|---|---|---|---|---|---|
| CLM-001 | registry 行は view marshalling 専用 — `consumedCtxFields` のみ参照、raw 計算 / fetch 禁止（A3 描画専用 + C9） | reviewed | high | - | - | 宣言 widget の妥当性は consumedCtxFields 整合 (frontmatterSyncGuard) と R:tag/G8 mechanism (responsibilityTagGuard) で構造的に担保。widget 個別 test は冗長 |
| CLM-002 | visibility predicate で空データ時の dead UI 回避（runtime 空状態防御） | reviewed | medium | - | - | 空状態到達経路は load 中 / fallback / partial 等多岐。widget 個別の predicate test より consumed readModel/calc layer の null 安全契約で構造的保証 |
| CLM-003 | DailyInventoryView wrap で日別推定在庫テーブル描画、CALC-003 computeEstimatedInventory 出力を直接 mapping（permanent floor）（widget は orchestration のみ、4 層依存遵守） | guarded | high | - | app/src/test/guards/presentationIsolationGuard.test.ts | - |

## 7. Co-Change Impact

- `renderDailyInventoryTable` の signature 変更
- `computeEstimatedInventory` の契約変更（helper 経由で連鎖）

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `storeDailyLaneSurfaceGuard`（permanent floor 1 件 = `computeEstimatedInventory` 経由の `result.daily` 直接 iterate、inquiry/01 §B-5）

## 9. Pipeline Concerns / Upstream Requests

- WID-009 と同構造の概要 concern
- **permanent floor の存在**: `computeEstimatedInventory` が `result.daily` を直接 iterate する必要があるため、`storeDailyLaneSurfaceGuard` の baseline 1 が intentional に残されている（inquiry/01 §B-5 で事実記録済）。本 widget がその唯一の consumer。該当責務層: domain（在庫推定の lane 経由化可否検討）
