---
id: UIC-003
kind: ui-component
exportName: KpiGrid
sourceRef: app/src/presentation/components/common/KpiCard.styles.ts
sourceLine: 168
category: kpi-layout
hooks: []
children: []
sideEffects: []
states:
  - ready
stories:
  - app/src/stories/KpiCard.stories.tsx
visualTests:
  - 'app/e2e/visual-regression.spec.ts#common-kpicard--dashboard-grid'
lifecycleStatus: active
replacedBy: null
supersedes: null
sunsetCondition: null
deadline: null
lastSourceCommit: 136e87d9e1b27268fa98c7dc4b5b27fcbd83d967
owner: implementation
specVersion: 1
---

# UIC-003 — KPI Grid (KpiCard 並びレイアウト)

## 1. 概要

`KpiGrid` は **styled-components で生成された CSS Grid 容器**で、複数の `KpiCard`（UIC-002）を統一間隔・列数で並べるレイアウト primitive（`KpiCard.styles.ts` 由来、`KpiCard.tsx` から re-export）。

## 2. Props Contract

- `KpiGrid` は `styled.div` の薄い wrapper、props は HTML div 標準のみ（`className` / `style` / `children`）
- 列数 / gap は CSS 内で fix（responsive grid）
- 子は通常 `KpiCard` 並び（4 件 × 1 行 等）

## 3. State Ownership

- なし（pure styled wrapper）

## 4. Hook Dependencies

- なし

## 5. Side Effects

- なし

## 6. State Coverage

| state | 振る舞い |
|---|---|
| ready | 子 children を grid 配置（responsive、子が 0 件でも空 grid 表示）|
| empty/loading/error は子 KpiCard 側に閉じる（KpiGrid は容器のみ）|

## 7. Visual Evidence

- **間接 cover**: `KpiCard.stories.tsx` の `DashboardGrid` story が `KpiGrid` で 4 KpiCard をラップしており、grid layout 安定性は同 story の visual regression (`common-kpicard--dashboard-grid`) で検証される（CLM-002 と整合）
- AR-CONTENT-SPEC-VISUAL-EVIDENCE guard で coverage ratchet-down 管理対象（KpiCard story を indirect evidence として登録）
- 単独 visual baseline は不要（KpiGrid は styled-components 薄 wrapper で、KpiCard 配置時の見た目が drift signal の正本）

### Behavior Claims (Phase J Evidence Level)

| ID | claim | evidenceLevel | riskLevel | tests | guards | verificationNote |
|---|---|---|---|---|---|---|
| CLM-001 | KpiCard の grid layout primitive (CSS-in-JS、KpiCard と pair 配置で常用) | reviewed | low | - | - | CSS-in-JS layout primitive の正しさは visual regression が唯一の信頼経路。indirect cover via KpiCard.stories で baseline 化済 |
| CLM-002 | KpiCard の Visual regression 経由で間接的に layout 安定性検証（独立 visual baseline は Phase G 検討中） | reviewed | medium | - | - | indirect cover の妥当性は KpiCard.stories の DashboardGrid story で機械的に保証 (UIC-002 と pair で baseline 化) |
| CLM-003 | presentation isolation - infrastructure / domain への直接 import 禁止（styles.ts は pure CSS-in-JS、4 層依存遵守） | guarded | high | - | app/src/test/guards/presentationIsolationGuard.test.ts | - |

## 8. Consumers

- `KpiCard` と pair で常用:
  - `CostDetailKpiSummaryWidget.tsx` (4 KpiCard 並び)
  - `DashboardPage.tsx` 系 KPI section
  - `Reports` / `Insight` ページの KPI 並び

## 9. Co-Change Impact

- `KpiCard.styles.ts` の column 数 / gap 値変更 → 全 consumer の見た目変更
- `KpiGrid` を `KpiCard.tsx` から `tables.ts` 経由 re-export している経路変更 → import 経路修正

## 10. Guard / Rule References

- `presentationIsolationGuard.test.ts` — presentation isolation
- `responsibilityTagGuard.test.ts` — R:component
- AR-CONTENT-SPEC-LIFECYCLE-FIELDS — 本 spec の lifecycle 整合
