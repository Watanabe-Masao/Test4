---
id: WID-032
kind: widget
widgetDefId: insight-budget
contextType: Unified
registry: INSIGHT_WIDGETS
registrySource: app/src/presentation/pages/Insight/widgets.tsx
registryLine: 59
group: インサイト
size: full
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields:
  - insightData
  - onExplain
  - result
consumedReadModels: []
consumedQueryHandlers: []
children:
  - BudgetTabContent
lastVerifiedCommit: f3226c0ac1d7c3f14598e36cce1b545a2725ca13
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-032 — 予算と実績（Insight）

## 1. 概要

`BudgetTabContent` に `insightData` / `result` / `onExplain` を forward する Insight page widget（Insight/widgets.tsx:14-24）。`isVisible` + render 内で **二重 null check**（`insightData != null`）。

## 2. Data Acquisition Path

- `isVisible: (ctx) => ctx.insightData != null` で gate
- render 内で `if (!ctx.insightData) return null`（isVisible と重複）
- 通過時に `<BudgetTabContent d={ctx.insightData} r={ctx.result} onExplain={ctx.onExplain} />`

## 3. Context Fields Consumed — 解説

- 3 field touch
- `insightData` は UnifiedWidgetContext の page-local optional（inquiry/04 §B-2）

## 4. Composition

- 直接の子: `BudgetTabContent`（`./InsightTabBudget` から import、:9）

## 5. Visibility

- 条件: `(ctx) => ctx.insightData != null`
- render 内でも再 check（二重）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- 予算・実績の計算不変は子 `BudgetTabContent` + `useInsightData`（inquiry/06 §A-1）に従属

### Behavior Claims (Phase J Evidence Level)

| ID | claim | evidenceLevel | riskLevel | tests | guards | verificationNote |
|---|---|---|---|---|---|---|
| CLM-001 | registry 行は view marshalling 専用 — `consumedCtxFields` のみ参照、raw 計算 / fetch 禁止（A3 描画専用 + C9） | reviewed | high | - | - | 宣言 widget の妥当性は consumedCtxFields 整合 (frontmatterSyncGuard) と R:tag/G8 mechanism (responsibilityTagGuard) で構造的に担保。widget 個別 test は冗長 |
| CLM-002 | visibility predicate で空データ時の dead UI 回避（runtime 空状態防御） | reviewed | medium | - | - | 空状態到達経路は load 中 / fallback / partial 等多岐。widget 個別の predicate test より consumed readModel/calc layer の null 安全契約で構造的保証 |
| CLM-003 | BudgetInsightView wrap で Insight ページの予算サマリ、RM-001 / CALC-011 統合表示（widget は orchestration のみ、4 層依存遵守） | guarded | high | - | app/src/test/guards/presentationIsolationGuard.test.ts | - |

## 7. Co-Change Impact

- `InsightData` 型の変更
- `BudgetTabContent` の props shape 変更（`d` / `r` / `onExplain`）
- `insightData` field の optional/required 状態変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`

## 9. Pipeline Concerns / Upstream Requests

- **isVisible + render 内の二重 null check**: `insightData != null` を 2 箇所（isVisible + render if）で判定。WID-040 / WID-031 と同構造の反復（inquiry/04 §D 17 gate widget の一例）
- **page-local optional を Unified ctx に配置**: `insightData` は Insight page 専用 field だが Unified ctx に optional 配置（inquiry/04 §B-2 該当）
- **prop 名の略記**: `d={ctx.insightData}` / `r={ctx.result}` の 1 文字 prop 名（WID-017 の `r` alias とも重なる）。該当責務層: presentation（命名規約）
