---
id: WID-037
kind: widget
widgetDefId: insight-pi-cv-map
contextType: Unified
registry: INSIGHT_WIDGETS
registrySource: app/src/presentation/pages/Insight/widgets.tsx
registryLine: 114
group: 構造分析
size: full
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields:
  - currentDateRange
  - queryExecutor
  - queryExecutor.isReady
  - selectedStoreIds
consumedReadModels: []
consumedQueryHandlers: []
children:
  - PiCvBubbleChart
lastVerifiedCommit: b23c49a
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-037 — カテゴリベンチマーク（Insight、DuckDB）

## 1. 概要

Unified ctx で DuckDB 3 点セット + isReady gate + render 内 **二重 null check**（`queryExecutor` と `currentDateRange` の両方）を行う registry entry（Insight/widgets.tsx:74-89）。INSIGHT_WIDGETS に登録だが実体は DuckDB chart。

## 2. Data Acquisition Path

- `isVisible: (ctx) => ctx.queryExecutor?.isReady === true` で gate
- render 内で `if (!ctx.queryExecutor || !ctx.currentDateRange) return null`（2 field の null check）

## 3. Context Fields Consumed — 解説

- DuckDB 3 点セット（Unified ctx 版）
- Unified ctx で `queryExecutor` / `currentDateRange` は optional のため、render 内で補助 null check が必要

## 4. Composition

- 直接の子: `PiCvBubbleChart`（`@/presentation/components/charts`、:8）

## 5. Visibility

- 条件: isVisible の isReady + render 内の 2 field null check（多重）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない

## 7. Co-Change Impact

- DuckDB 3 点の型変更
- `PiCvBubbleChart` の props shape 変更
- `UnifiedWidgetContext` の `queryExecutor` / `currentDateRange` optional 状態変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `queryPatternGuard`

## 9. Pipeline Concerns / Upstream Requests

- **Dashboard 固有 optional field を Unified widget が使用**: `queryExecutor` / `currentDateRange` / `selectedStoreIds` は Dashboard `WidgetContext` では required 昇格されているが、Unified では optional。本 widget（型 B Unified）が Dashboard 固有 optional を touch する 1 例（inquiry/04 §B-5 特記事項）
- **isVisible + render 内 null check の 3 重防御**: isVisible の `isReady` + render の `!queryExecutor || !currentDateRange` という 2 箇所での null check。該当責務層: application
- **DuckDB 3 点セット pattern の 7 例目**（WID-024-029 + 本 widget）
