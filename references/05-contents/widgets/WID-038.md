---
id: WID-038
kind: widget
widgetDefId: category-total-view
contextType: Unified
registry: CATEGORY_WIDGETS
registrySource: app/src/presentation/pages/Category/widgets.tsx
registryLine: 59
group: カテゴリ分析
size: full
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields:
  - onCustomCategoryChange
  - onExplain
  - result
  - selectedResults
  - settings
  - stores
consumedReadModels: []
consumedQueryHandlers: []
children:
  - CategoryTotalView
lastVerifiedCommit: b23c49a
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-038 — カテゴリ合計分析

## 1. 概要

`CategoryTotalView` に 6 field を forward する Category page widget（Category/widgets.tsx:14-28）。`r` 1 文字 alias + optional 2 field の `??` default 適用。

## 2. Data Acquisition Path

- 6 field を個別 props で forward
- `selectedResults ?? []` / `onCustomCategoryChange ?? (() => {})` で default 適用

## 3. Context Fields Consumed — 解説

- 6 field touch（page-local 2 + core 4）
- `selectedResults` / `onCustomCategoryChange` は Category page-local optional（inquiry/04 §B-2）
- default 値を registry 行で `?? []` / `?? (() => {})` として実装

## 4. Composition

- 直接の子: `CategoryTotalView`（`./CategoryTotalView` から import、:9）
- `features/category/ui/CategoryTotalView.tsx` の duplicate source（inquiry/01 §特殊）
- `unifiedRegistry.ts` は pages 版を import

## 5. Visibility

- `isVisible` 未指定（常時可視）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- カテゴリ合計の集計不変は子 `CategoryTotalView` 側

## 7. Co-Change Impact

- `selectedResults: readonly StoreResult[]` / `storeNames` / `onCustomCategoryChange` の型変更
- `CategoryTotalView` の props shape 変更
- `AppSettings` の型変更（`settings` forward）

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`

## 9. Pipeline Concerns / Upstream Requests

- **registry 行で default 値 2 件を決定**: `selectedResults ?? []` / `onCustomCategoryChange ?? (() => {})` の 2 箇所で default 値が registry 層にハードコード。該当責務層: application（default 値の contract 化）
- **`prop r` 1 文字 alias**: WID-017 / WID-032 / WID-034 で見た 1 文字 prop 名の再発
- **page-local optional field 3 件を本 widget が参照**: `selectedResults` / `onCustomCategoryChange` + `storeNames`（WID-039 で参照）。Category page の page-local 3 field がほぼ全て 2 widget で使われている
