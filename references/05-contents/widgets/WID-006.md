---
id: WID-006
kind: widget
widgetDefId: chart-sales-purchase-comparison
contextType: Dashboard-local

registry: WIDGETS_CHART
registrySource: app/src/presentation/pages/Dashboard/widgets/registryChartWidgets.tsx
registryLine: 111

group: 構造分析
size: full
linkTo: null

acquisitionPath: ctx-direct

consumedCtxFields:
  - allStoreResults
  - stores
  - daysInMonth
  - storeDailyLane.bundle.currentSeries
  - chartPeriodProps.rangeStart
  - chartPeriodProps.rangeEnd
consumedReadModels: []
consumedQueryHandlers: []
children:
  - SalesPurchaseComparisonChart

lastVerifiedCommit: a62a7a7

owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23

specVersion: 1
---

# WID-006 — 売上・仕入 店舗比較

## 1. 概要

`allStoreResults.values()` を配列化し、2 店舗以上の場合のみ `SalesPurchaseComparisonChart` に comparison / stores / storeDailyLane を forward する registry entry（registryChartWidgets.tsx:111-130）。

## 2. Data Acquisition Path

- registry 行内で `Array.from(allStoreResults.values())` を実行し、`results.length < 2` で早期 null return
- `storeDailyLane?.bundle.currentSeries ?? null` で lane 参照（optional + default）
- `chartPeriodProps?.rangeStart` / `rangeEnd` を optional 転送

## 3. Context Fields Consumed — 解説

- 6 field touch（うち `storeDailyLane.bundle.currentSeries` / `chartPeriodProps.rangeStart` / `rangeEnd` は sub-path）
- registry 行で `Array.from(Map)` の配列化 + `length < 2` チェックを実行（inline logic）
- `storeDailyLane` は `ctx.storeDailyLane` optional、`.bundle.currentSeries` も optional なので二重 null-safe

## 4. Composition

- 直接の子: `SalesPurchaseComparisonChart`（`@/presentation/components/charts`）
- `storeDailyLane.bundle.currentSeries` を `storeDailySeries` prop として渡し、子は raw `StoreResult.daily` を触らず lane 経由の series を消費（`storeDailyLaneSurfaceGuard` 準拠）

## 5. Visibility

- `isVisible` 未指定（常時可視）
- ただし render 内で `results.length < 2` で **早期 null return**（widget 固有 gate）
- 実質的な可視条件は「2 店舗以上選択時のみ」

## 6. Invariants

- 2 店舗未満では render が null を返す（空 map + 1 店舗 map を同等扱い）
- 複数店舗の比較順序は `allStoreResults.values()` のイテレーション順（Map 挿入順）に従う

## 7. Co-Change Impact

- `allStoreResults: ReadonlyMap<string, StoreResult>` の型変更
- `SalesPurchaseComparisonChart` の `comparisonResults` / `stores` / `storeDailySeries` props 変更
- `StoreDailyBundle` / `storeDailyLane.bundle.currentSeries` の契約変更
- `chartPeriodProps` 型変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`
- `storeDailyLaneSurfaceGuard`（raw `result.daily` 直接 iterate 禁止、本 widget は lane 経由で準拠）
- `chartInputBuilderGuard` / `chartRenderingStructureGuard`

## 9. Pipeline Concerns / Upstream Requests

- **render 内の inline logic**: `Array.from(Map.values())` + `length < 2` 早期 null return + lane null coalescing を registry 行内で実行。registry 行が「登録」ではなく「変換ロジック」を含む事実。該当責務層: presentation（inline logic の抽出可否）
- **二重 null-safe の lane 参照**: `ctx.storeDailyLane?.bundle.currentSeries ?? null` で optional 2 段 + default を記述。lane 契約が optional のまま運用されている事実。該当責務層: application（lane contract の required 昇格検討）
- **`isVisible` 未指定 + render 内 gate の混在**: 一方で `isVisible` 未指定、他方で render 内の `< 2` 早期 return。表示制御の 2 経路が混在。該当責務層: presentation（gate 表現の統一）
