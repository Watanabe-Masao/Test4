---
id: WID-033
kind: widget
widgetDefId: insight-budget-simulator
contextType: Unified

registry: INSIGHT_WIDGETS
registrySource: app/src/presentation/pages/Insight/widgets.tsx
registryLine: 26

group: インサイト
size: full
linkTo: null

acquisitionPath: ctx-direct

consumedCtxFields:
  - result
consumedReadModels: []
consumedQueryHandlers: []
children:
  - BudgetSimulatorWidget

lastVerifiedCommit: b23c49a

owner: implementation
reviewCadenceDays: 60
lastReviewedAt: 2026-04-23

specVersion: 1
---

# WID-033 — 予算達成シミュレーター

## 1. 概要

`BudgetSimulatorWidget` に full ctx passthrough する registry entry（Insight/widgets.tsx:26-38）。**Unified core required `result` に対して null check + isVisible gate の 2 段防御**（core required なのに runtime で null 想定されている事実）。コメント記載: `InsightPage 以外 (Dashboard 等) でも利用可能`。

## 2. Data Acquisition Path

- `isVisible: (ctx) => ctx.result != null` で gate（Unified core required に対して null check）
- render 内で `if (!ctx.result) return null`（二重）
- 通過時に `<BudgetSimulatorWidget ctx={ctx} />`

## 3. Context Fields Consumed — 解説

- 1 field touch（`result`）だが子 `BudgetSimulatorWidget` が full ctx を使う想定
- core required field を null check する事実は型と runtime の乖離を示唆（inquiry/04 §D で特記）

## 4. Composition

- 直接の子: `BudgetSimulatorWidget`（`@/features/budget` から import、:11）
- features slice の UI entry point

## 5. Visibility

- 条件: `(ctx) => ctx.result != null`
- コメントで「`InsightPage 以外 (Dashboard 等) でも利用可能`」と page 非依存性を明示

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- Budget Simulator の計算不変は `BudgetSimulatorWidget` 内部 + `features/budget/application/pure/` に従属

## 7. Co-Change Impact

- `StoreResult` 型変更
- `BudgetSimulatorWidget` の props shape 変更
- `UnifiedWidgetContext.result` の required / optional 状態変更（現在 required だが null check あり）

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`

## 9. Pipeline Concerns / Upstream Requests

- **core required `result` への null check**: `UnifiedWidgetContext.result: StoreResult`（required）に対して `ctx.result != null` を 2 段 check。**型と runtime 実態の乖離**の典型例（inquiry/04 §D + inquiry/09 S3-H3 で特記）。該当責務層: domain / application（`result` required 保証の厳格化）
- **features/budget の widget が Insight page registry に登録される pattern**: `BudgetSimulatorWidget` は `@/features/budget` 由来だが `INSIGHT_WIDGETS` に登録。feature slice と page registry の境界が曖昧。該当責務層: presentation / features（widget 所属 page の規約）
- **コメントで page 非依存性を明示しているが実際は Insight registry 限定**: 「他 page でも利用可能」とあるが他 page の registry には登録されていない。設計意図と実装の gap
