---
id: WID-035
kind: widget
widgetDefId: insight-forecast
contextType: Unified

registry: INSIGHT_WIDGETS
registrySource: app/src/presentation/pages/Insight/widgets.tsx
registryLine: 51

group: インサイト
size: full
linkTo: null

acquisitionPath: ctx-direct

consumedCtxFields:
  - insightData.forecastData
  - insightData
  - result
  - onExplain
consumedReadModels: []
consumedQueryHandlers: []
children:
  - ForecastTabContent

lastVerifiedCommit: b23c49a

owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23

specVersion: 1
---

# WID-035 — 予測・パターン（Insight）

## 1. 概要

`ForecastTabContent` に `insightData` / `result` / `onExplain` を forward。`isVisible` + render 内で **sub-path null check**（`insightData?.forecastData != null`）（Insight/widgets.tsx:51-60）。

## 2. Data Acquisition Path

- `isVisible: (ctx) => ctx.insightData?.forecastData != null` で gate
- render 内で同等の null check
- 通過時に子へ forward

## 3. Context Fields Consumed — 解説

- 4 field touch（sub-path `insightData.forecastData` を別にカウント）
- `forecastData` が存在しないとき `insightData` 全体が null でないケースもありうる前提

## 4. Composition

- 直接の子: `ForecastTabContent`（`./InsightTabForecast` から import、:10）

## 5. Visibility

- 条件: `(ctx) => ctx.insightData?.forecastData != null`

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- 予測計算は `useInsightData` 内の `calculateForecast` に従属（inquiry/06 §A-1）

## 7. Co-Change Impact

- `InsightData.forecastData` の sub-field 変更
- `ForecastTabContent` の props shape 変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`

## 9. Pipeline Concerns / Upstream Requests

- **optional sub-path gate の初見**: WID-032 / WID-034 は `insightData != null`、本 widget は `insightData?.forecastData != null` と sub-path まで検証。InsightData 内部の部分ロード状態を widget 側で判定。該当責務層: application（`InsightData` の sub-field optional / required 契約を Zod 等で厳格化）
- **二重 null check の 4 例目**
