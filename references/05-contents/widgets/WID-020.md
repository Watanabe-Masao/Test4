---
id: WID-020
kind: widget
widgetDefId: analysis-causal-chain
contextType: Dashboard-local
registry: WIDGETS_ANALYSIS
registrySource: app/src/presentation/pages/Dashboard/widgets/registryAnalysisWidgets.tsx
registryLine: 130
group: 要因分析
size: full
linkTo:
  view: insight
  tab: decomposition
acquisitionPath: ctx-direct
consumedCtxFields:
  - prevYear
  - prevYear.discountRate
  - prevYear.hasPrevYear
  - prevYear.totalDiscountEntries
  - prevYear.totalSales
  - result
consumedReadModels: []
consumedQueryHandlers: []
children:
  - CausalChainExplorer
lastVerifiedCommit: b23c49a
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-020 — 因果チェーン分析

## 1. 概要

`CausalChainExplorer` に `result` full object + 前年データを加工した**7 フィールドの inline object literal** を forward する registry entry（registryAnalysisWidgets.tsx:110-133）。

## 2. Data Acquisition Path

- registry 行で `prevYear.hasPrevYear` 判定 → inline object literal 構築 or undefined
- object literal 内で `grossProfitRate: null` / `costRate: null` / `costInclusionRate: null` を hardcode null
- `extractPrevYearCustomerCount(ctx.prevYear)` を `@/features/comparison` から call

## 3. Context Fields Consumed — 解説

- `result` は object 全体を child に forward
- `prevYear.hasPrevYear` / `discountRate` / `totalDiscountEntries` / `totalSales` の 4 sub-path を touch
- `extractPrevYearCustomerCount` は features/ helper

## 4. Composition

- 直接の子: `CausalChainExplorer`（`@/presentation/components/charts`）
- 外部 import: `extractPrevYearCustomerCount`（`@/features/comparison`）

## 5. Visibility

- `isVisible` 未指定（常時可視）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- 因果チェーンの不変は子 `CausalChainExplorer` 側

## 7. Co-Change Impact

- `PrevYearData` の `discountRate` / `totalDiscountEntries` / `totalSales` 型変更
- `CausalChainExplorer` の `prevYearData` props shape 変更
- `extractPrevYearCustomerCount` signature 変更
- `StoreResult` 全体の型変更（object passthrough のため）

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`
- `factorDecompositionPathGuard`

## 9. Pipeline Concerns / Upstream Requests

- **registry 行内の inline object literal 構築**: 7 フィールド（`grossProfitRate` / `costRate` / `discountRate` / `costInclusionRate` / `discountEntries` / `totalSales` / `totalCustomers`）を registry 行で **object literal として組み立て** + 3 フィールド hardcode null。該当責務層: application（`CausalChainExplorer` 向け input builder を extract）
- **`null` hardcode の意味不明**: `grossProfitRate: null` / `costRate: null` / `costInclusionRate: null` が null で固定されている理由がコード上で明示されていない。該当責務層: domain（`CausalChainExplorer` の契約で null 扱いの正式 contract 化）
- **`@/features/comparison` の直接 import**: registry 行が features 側 helper を直接 import（WID-001 `ConditionSummaryEnhanced` と同 pattern）。該当責務層: application（comparison readModel への移設可否）
