---
id: WID-039
kind: widget
widgetDefId: category-comparison-view
contextType: Unified
registry: CATEGORY_WIDGETS
registrySource: app/src/presentation/pages/Category/widgets.tsx
registryLine: 76
group: カテゴリ分析
size: full
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields:
  - selectedResults
  - selectedResults.length
  - storeNames
consumedReadModels: []
consumedQueryHandlers: []
children:
  - CategoryComparisonView
lastVerifiedCommit: f3226c0ac1d7c3f14598e36cce1b545a2725ca13
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-039 — 店舗間比較（Category）

## 1. 概要

`CategoryComparisonView` に `selectedResults` / `storeNames` を forward、**型 assertion + default** を registry 行で実装、`isVisible` で `selectedResults?.length > 1` を gate（Category/widgets.tsx:30-41）。

## 2. Data Acquisition Path

- `(ctx.selectedResults ?? []) as StoreResult[]` で default + 型 assertion
- `(ctx.storeNames ?? new Map()) as Map<string, string>` で default + 型 assertion
- `isVisible: (ctx) => (ctx.selectedResults?.length ?? 0) > 1` で 2 店舗以上 gate

## 3. Context Fields Consumed — 解説

- 2 field touch
- 両方とも Category page-local optional（WID-038 と同じ）

## 4. Composition

- 直接の子: `CategoryComparisonView`（`./CategoryComparisonView` から import、:10）

## 5. Visibility

- 条件: `selectedResults?.length > 1`（2 店舗以上）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- 店舗間比較の計算不変は子側

### Behavior Claims (Phase J Evidence Level)

| ID | claim | evidenceLevel | riskLevel | tests | guards | verificationNote |
|---|---|---|---|---|---|---|
| CLM-001 | registry 行は view marshalling 専用 — `consumedCtxFields` のみ参照、raw 計算 / fetch 禁止（A3 描画専用 + C9） | reviewed | high | - | - | 宣言 widget の妥当性は consumedCtxFields 整合 (frontmatterSyncGuard) と R:tag/G8 mechanism (responsibilityTagGuard) で構造的に担保。widget 個別 test は冗長 |
| CLM-002 | visibility predicate で空データ時の dead UI 回避（runtime 空状態防御） | reviewed | medium | - | - | 空状態到達経路は load 中 / fallback / partial 等多岐。widget 個別の predicate test より consumed readModel/calc layer の null 安全契約で構造的保証 |
| CLM-003 | CategoryComparisonView wrap でカテゴリ比較 view、freePeriod + 期間切替 read-model 経由（widget は orchestration のみ、4 層依存遵守） | guarded | high | - | app/src/test/guards/presentationIsolationGuard.test.ts | - |

## 7. Co-Change Impact

- `StoreResult` / `ReadonlyMap<string, string>` の型変更
- `CategoryComparisonView` の props shape 変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`

## 9. Pipeline Concerns / Upstream Requests

- **型 assertion（`as StoreResult[]` / `as Map<string, string>`）**: `readonly StoreResult[]` と `ReadonlyMap` を mutable 型にキャストしている（子側の型契約と合わせるため）。該当責務層: presentation / features（子 component の props 型を readonly に揃える可否）
- **default + 型 assertion の 2 段組み合わせ**: registry 層で「null 吸収」+「型変換」を同時に実施。該当責務層: application（default 値の契約統一）
- **length > 1 の 2 段 null-safe**: `selectedResults?.length ?? 0) > 1` の書き方（WID-006 の `< 2` 早期 return と異なる表現で同等の意味）
