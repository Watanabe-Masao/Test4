---
id: WID-013
kind: widget
widgetDefId: exec-store-kpi
contextType: Dashboard-local

registry: WIDGETS_EXEC
registrySource: app/src/presentation/pages/Dashboard/widgets/registryExecWidgets.tsx
registryLine: 58

group: 構造分析
size: full
linkTo:
  view: reports

acquisitionPath: ctx-direct

consumedCtxFields: []
consumedReadModels: []
consumedQueryHandlers: []
children:
  - "renderStoreKpiTable (TableWidgets)"

lastVerifiedCommit: b23c49a

owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23

specVersion: 1
---

# WID-013 — 店舗別KPI一覧

## 1. 概要

`renderStoreKpiTable(ctx)` 関数に ctx を委譲する registry entry（registryExecWidgets.tsx:58-64）。`linkTo: { view: 'reports' }` で Reports page へ。

## 2. Data Acquisition Path

- registry 行では `renderStoreKpiTable(ctx)` を呼び出すのみ

## 3. Context Fields Consumed — 解説

- registry 行からの直接 touch 0
- 実 ctx 依存は `renderStoreKpiTable` 側（`allStoreResults` / `stores` 等を touch 想定）

## 4. Composition

- render helper: `renderStoreKpiTable`（`./TableWidgets` から import、:9）

## 5. Visibility

- `isVisible` 未指定（常時可視）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない

## 7. Co-Change Impact

- `renderStoreKpiTable` の signature 変更
- `linkTo` の view 型変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`
- `pageMetaGuard`

## 9. Pipeline Concerns / Upstream Requests

- WID-009 と同構造の委譲パターン concern
- **linkTo Dashboard → Reports 跨ぎ**: 型 A → 型 B（Reports = Unified）の linkTo。WID-002 / WID-011 と同じ跨ぎパターンの 3 例目
