---
id: WID-021
kind: widget
widgetDefId: analysis-sensitivity
contextType: Dashboard-local
registry: WIDGETS_ANALYSIS
registrySource: app/src/presentation/pages/Dashboard/widgets/registryAnalysisWidgets.tsx
registryLine: 156
group: 予測・シミュレーション
size: full
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields:
  - readModels.customerFact
  - result
consumedReadModels:
  - customerFact
consumedQueryHandlers: []
children:
  - SensitivityDashboard
lastVerifiedCommit: b23c49a
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-021 — 感度分析ダッシュボード

## 1. 概要

`SensitivityDashboard` に `result` full object + IIFE 経由の `customerCount` を forward（registryAnalysisWidgets.tsx:135-148）。

## 2. Data Acquisition Path

- registry 行で IIFE `(() => { const cf = ctx.readModels?.customerFact; return cf?.status === 'ready' ? cf.data.grandTotalCustomers : undefined })()`
- `result` は object 全体を forward

## 3. Context Fields Consumed — 解説

- `result` object 全体
- `readModels.customerFact` の ready 判定 + data 取得（WID-018 と同 pattern）

## 4. Composition

- 直接の子: `SensitivityDashboard`（`@/presentation/components/charts`）

## 5. Visibility

- `isVisible` 未指定（常時可視）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- 感度分析の計算不変は子側

## 7. Co-Change Impact

- `StoreResult` 全体の型変更
- `readModels.customerFact` の status / data 構造変更
- `SensitivityDashboard` の props shape 変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`
- `customerFactPathGuard`

## 9. Pipeline Concerns / Upstream Requests

- **IIFE pattern の 3 例目**: WID-018 の 2 回、本 widget の 1 回。合計 3 箇所で同 pattern（readModels.customerFact の ready → data.grandTotalCustomers、else undefined）。該当責務層: application（共通 selector の抽出）
- **WID-018 との `customerCount` fallback 差異**: WID-018 は `result.totalCustomers` を fallback に使うが、本 widget は `undefined` を fallback。同じ readModel に対する fallback 戦略が widget 間で揺れている。該当責務層: application
