---
id: WID-017
kind: widget
widgetDefId: analysis-customer-scatter
contextType: Dashboard-local
registry: WIDGETS_ANALYSIS
registrySource: app/src/presentation/pages/Dashboard/widgets/registryAnalysisWidgets.tsx
registryLine: 61
group: トレンド分析
size: full
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields:
  - daysInMonth
  - month
  - prevYear
  - result
  - year
consumedReadModels: []
consumedQueryHandlers: []
children:
  - CustomerScatterChart
lastVerifiedCommit: b23c49a
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-017 — 客数×客単価 効率分析

## 1. 概要

`CustomerScatterChart` に daily / year / month / prevYear を個別 props で forward する registry entry（registryAnalysisWidgets.tsx:40-53）。**render 引数で destructuring** `{ result: r, daysInMonth, year, month, prevYear }` を使用。

## 2. Data Acquisition Path

- registry 行では render 引数で destructuring + 5 field を props として forward
- `prevYear.hasPrevYear ? prevYear.daily : undefined` で optional 転送

## 3. Context Fields Consumed — 解説

- 6 field touch（destructuring で取得）
- `result` は `r` alias で受け取り、`r.daily` を forward
- WID-003 と類似構造だが、chartPeriodProps / targetRate / warningRate は参照しない

## 4. Composition

- 直接の子: `CustomerScatterChart`（`@/presentation/components/charts`）

## 5. Visibility

- `isVisible` 未指定（常時可視）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- 客数 × 客単価の計算不変は子 chart 側 + `calculateTransactionValue` 等 domain 関数に従属

## 7. Co-Change Impact

- `StoreResult.daily` / `PrevYearData` の型変更
- `CustomerScatterChart` の props shape 変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`
- `chartInputBuilderGuard` / `chartRenderingStructureGuard`

## 9. Pipeline Concerns / Upstream Requests

- **render 引数の destructuring pattern**: `({ result: r, daysInMonth, year, month, prevYear })` の分解代入。他 widget（例 WID-003 `(ctx) => ...ctx.result.daily...`）と書き方が揺れている。該当責務層: presentation（registry 行の記述スタイル統一可否）
- **`prevYear.hasPrevYear` 判定の registry 行内**: optional を conditional で undefined 転送する pattern（WID-002 / WID-003 と同じ）。該当責務層: application（`prevYear` 型の null-object pattern 検討）
