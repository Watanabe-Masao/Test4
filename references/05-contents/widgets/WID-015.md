---
id: WID-015
kind: widget
widgetDefId: analysis-waterfall
contextType: Dashboard-local
registry: WIDGETS_ANALYSIS
registrySource: app/src/presentation/pages/Dashboard/widgets/registryAnalysisWidgets.tsx
registryLine: 29
group: 要因分析
size: full
linkTo:
  view: insight
  tab: decomposition
acquisitionPath: ctx-direct
consumedCtxFields:
  - result
  - storeKey
consumedReadModels: []
consumedQueryHandlers: []
children:
  - WaterfallChartWidget
lastVerifiedCommit: f3226c0
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-015 — 粗利ウォーターフォール

## 1. 概要

`WaterfallChartWidget` に full ctx passthrough + storeKey を React key として指定（registryAnalysisWidgets.tsx:20-26）。`linkTo: { view: 'insight', tab: 'decomposition' }` で Insight 遷移。

## 2. Data Acquisition Path

- registry 行では `<WaterfallChartWidget key={ctx.storeKey} ctx={ctx} />` の形

## 3. Context Fields Consumed — 解説

- `storeKey` は React key 用のみ
- 実データ依存は子 `WaterfallChartWidget` 内部に散在

## 4. Composition

- 直接の子: `WaterfallChartWidget`（`./WaterfallChart` から import、:12）

## 5. Visibility

- `isVisible` 未指定（常時可視）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- Waterfall の要因分解不変は子側の decompose / shapley 計算に従属（invariant-catalog の要因分解 INV 系）

### Behavior Claims (Phase J Evidence Level)

| ID | claim | evidenceLevel | riskLevel | tests | guards | verificationNote |
|---|---|---|---|---|---|---|
| CLM-001 | registry 行は view marshalling 専用 — `consumedCtxFields` のみ参照、raw 計算 / fetch 禁止（A3 描画専用 + C9） | reviewed | high | - | - | 宣言 widget の妥当性は consumedCtxFields 整合 (frontmatterSyncGuard) と R:tag/G8 mechanism (responsibilityTagGuard) で構造的に担保。widget 個別 test は冗長 |
| CLM-002 | visibility predicate で空データ時の dead UI 回避（runtime 空状態防御） | reviewed | medium | - | - | 空状態到達経路は load 中 / fallback / partial 等多岐。widget 個別の predicate test より consumed readModel/calc layer の null 安全契約で構造的保証 |
| CLM-003 | WaterfallChart wrap で要因分解の瀑布グラフ描画、RM-006 calculateFactorDecomposition (Shapley) 出力を chart 入力に mapping（widget は orchestration のみ、4 層依存遵守） | guarded | high | - | app/src/test/guards/presentationIsolationGuard.test.ts | - |

## 7. Co-Change Impact

- `storeKey` / `WidgetContext` 型変更
- `WaterfallChartWidget` の props shape 変更
- `linkTo` の view / tab 型変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`
- `factorDecompositionPathGuard`（子が要因分解 readModel を経由する場合）
- `pageMetaGuard`

## 9. Pipeline Concerns / Upstream Requests

- **full ctx passthrough の静的解析不能**（汎用 concern）
- **`storeKey` React key pattern 5 例目**（WID-008/014 + 本 + WID-016 ほか）
- **Dashboard → Insight/decomposition tab 跨ぎ linkTo**: 型 A → 型 B の linkTo + tab。WID-002/003/011/013/014 に続く 6 例目
