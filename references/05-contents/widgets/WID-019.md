---
id: WID-019
kind: widget
widgetDefId: analysis-category-pi
contextType: Dashboard-local
registry: WIDGETS_ANALYSIS
registrySource: app/src/presentation/pages/Dashboard/widgets/registryAnalysisWidgets.tsx
registryLine: 107
group: トレンド分析
size: full
linkTo:
  view: category
acquisitionPath: ctx-direct
consumedCtxFields:
  - prevYearScope
  - readModels.customerFact
  - result.totalCustomers
consumedReadModels:
  - customerFact
consumedQueryHandlers: []
children:
  - CategoryPerformanceChart
lastVerifiedCommit: b23c49a
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-019 — カテゴリPI値・偏差値（常時非可視）

## 1. 概要

`isVisible: () => false` で**常時非可視**の registry entry（registryAnalysisWidgets.tsx:88-107）。コメント `// PerformanceIndexChart に統合済み — 非表示` が明示。render 関数本体は存在するが実行経路に入らない。

## 2. Data Acquisition Path

- **実行されない**（isVisible false で render 不呼び出し）
- 仮に render されたとしても、`CategoryPerformanceChart` に `categoryData={null}` / `isLoading={false}` を hardcode 渡し

## 3. Context Fields Consumed — 解説

- render 内部で 3 field 参照があるが実行されないため実 touch 0
- `totalCustomers` は WID-018 と同形式の IIFE で導出

## 4. Composition

- 直接の子: `CategoryPerformanceChart`（`@/presentation/components/charts`）
- 常時 `categoryData={null}` + `onLevelChange={() => {}}` を渡す（実質 stub）

## 5. Visibility

- `isVisible: () => false`（常時非可視）
- コメント: 「PerformanceIndexChart に統合済み — 非表示」

## 6. Invariants

- 本 widget は実行経路に入らないため invariant 適用対象外
- ただし registry entry としては 45 件のカウントに含まれる

## 7. Co-Change Impact

- 実行されないため runtime 影響は極小
- `CategoryPerformanceChart` の props shape 変更時、hardcoded 値の型整合は必要

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`

## 9. Pipeline Concerns / Upstream Requests

- **常時非可視 widget の residual registration**: `isVisible: () => false` で実行されないが registry entry は残存。**dead code 同然だが削除されていない**事実。該当責務層: presentation（registry cleanup、orphan 検出対象 = inquiry/12 J7 関連）
- **「統合済み」コメントによる削除猶予**: コメントで「統合済み」と記載されているが、実 registry からの除去には至っていない。inquiry/10 §D-3「中間状態固定化」の典型例
- **`categoryData={null}` + `onLevelChange={() => {}}` hardcode**: 非可視 widget であっても render signature を保つために stub 値が registry 行に埋没している
