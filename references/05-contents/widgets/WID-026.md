---
id: WID-026
kind: widget
widgetDefId: duckdb-category-mix
contextType: Dashboard-local
registry: WIDGETS_DUCKDB
registrySource: app/src/presentation/pages/Dashboard/widgets/registryDuckDBWidgets.tsx
registryLine: 35
group: トレンド分析
size: full
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields:
  - currentDateRange
  - loadedMonthCount
  - queryExecutor
  - queryExecutor.isReady
  - selectedStoreIds
consumedReadModels: []
consumedQueryHandlers: []
children:
  - CategoryMixChart
lastVerifiedCommit: b23c49a
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-026 — カテゴリ構成比推移（DuckDB）

## 1. 概要

`CategoryMixChart` に DuckDB 3 点セット forward + `isVisible` で **2 段 predicate** `queryExecutor.isReady === true && loadedMonthCount >= 2` を gate する registry entry（registryDuckDBWidgets.tsx:29-41）。

## 2. Data Acquisition Path

- DuckDB 3 点セット forward
- `isVisible` predicate が 2 条件 AND

## 3. Context Fields Consumed — 解説

- 4 field touch（DuckDB 3 + `loadedMonthCount`）
- `loadedMonthCount >= 2` は「複数月データがロード済み」の判定

## 4. Composition

- 直接の子: `CategoryMixChart`（`@/presentation/components/charts`）

## 5. Visibility

- 条件: `(ctx) => ctx.queryExecutor?.isReady === true && ctx.loadedMonthCount >= 2`
- 本 registry 内で唯一の 2 条件 AND predicate

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- `loadedMonthCount >= 2` が真なら複数月 data が存在する（application 層の load 契約）

### Behavior Claims (Phase J Evidence Level)

| ID | claim | evidenceLevel | riskLevel | tests | guards |
|---|---|---|---|---|---|
| CLM-001 | registry 行は view marshalling 専用 — `consumedCtxFields` のみ参照、raw 計算 / fetch 禁止（A3 描画専用 + C9）| reviewed | high | - | - |
| CLM-002 | visibility predicate で空データ時の dead UI 回避（runtime 空状態防御）| reviewed | medium | - | - |
| CLM-003 | CategoryMixChart wrap でカテゴリ ミックス描画、freePeriod read-model 経由でデータ取得（widget は orchestration のみ、4 層依存遵守）| guarded | high | - | app/src/test/guards/presentationIsolationGuard.test.ts |

## 7. Co-Change Impact

- `loadedMonthCount: number` 型変更
- DuckDB 3 点の型変更
- `CategoryMixChart` の props shape 変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `queryPatternGuard`

## 9. Pipeline Concerns / Upstream Requests

- **2 段 predicate の唯一事例**: 他 DuckDB widget は 1 条件（`isReady === true`）のみ。本 widget のみ `loadedMonthCount >= 2` を追加。該当責務層: application（複数月必要の意味論を widget 側で判断する妥当性）
- **`loadedMonthCount` が registry 行 surface**: 月数ロード状態が presentation 最表層で判定されている。該当責務層: application（load state の readModel 化検討）
