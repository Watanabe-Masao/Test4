---
id: WID-024
kind: widget
widgetDefId: analysis-duckdb-features
contextType: Dashboard-local
registry: WIDGETS_ANALYSIS
registrySource: app/src/presentation/pages/Dashboard/widgets/registryAnalysisWidgets.tsx
registryLine: 189
group: トレンド分析
size: full
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields:
  - currentDateRange
  - queryExecutor
  - queryExecutor.isReady
  - selectedStoreIds
consumedReadModels: []
consumedQueryHandlers: []
children:
  - FeatureChart
lastVerifiedCommit: b23c49a
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-024 — 売上トレンド分析（DuckDB）

## 1. 概要

`FeatureChart` に DuckDB クエリ経路 3 field（`queryExecutor` / `currentDateRange` / `selectedStoreIds`）を forward する registry entry（registryAnalysisWidgets.tsx:168-180）。`isVisible` で `queryExecutor?.isReady === true` を gate。

## 2. Data Acquisition Path

- `queryExecutor` / `currentDateRange` / `selectedStoreIds` を個別 props で子に forward
- isVisible で DuckDB ready 判定

## 3. Context Fields Consumed — 解説

- 3 field touch（DuckDB 3 点セット）
- この 3 点セットは WID-025〜WID-029（WIDGETS_DUCKDB 全 5 件）+ WID-037（`insight-pi-cv-map`）と同じ touch pattern

## 4. Composition

- 直接の子: `FeatureChart`（`@/presentation/components/charts`）
- 子内部で DuckDB query を発行して `FeatureChart` を render する想定

## 5. Visibility

- 条件: `(ctx) => ctx.queryExecutor?.isReady === true`
- `queryExecutor` が null or `.isReady !== true` の場合は非可視

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- `queryExecutor.isReady === true` が真のときのみ children が DuckDB query を発行（契約）

## 7. Co-Change Impact

- `QueryExecutor` interface の `isReady` 型変更
- `DateRange` / `ReadonlySet<string>` 等 3 point の型変更
- `FeatureChart` の props shape 変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`
- `queryPatternGuard`（DuckDB クエリ発行経路の強制）

## 9. Pipeline Concerns / Upstream Requests

- **DuckDB 3 点セット（`queryExecutor` / `currentDateRange` / `selectedStoreIds`）の widget 層 surface**: 本 widget 含む 7 widget（WID-024-029, WID-037）が同じ 3 field を個別に forward。該当責務層: application（DuckDB 経路の input を束ねる adapter / bundle 検討）
- **`queryExecutor?.isReady === true` null-safe gate の 7 例目**: WID-024/025/026/027/028/029/037 が同じ predicate pattern。inquiry/04 §D の gate pattern 定着の典型
- **本 widget は WIDGETS_ANALYSIS 登録だが DuckDB 経路**: WIDGETS_DUCKDB registry があるにもかかわらず `analysis-duckdb-features` を `WIDGETS_ANALYSIS` に置いている。registry 分類の境界が曖昧
