---
id: WID-036
kind: widget
widgetDefId: insight-decomposition
contextType: Unified
registry: INSIGHT_WIDGETS
registrySource: app/src/presentation/pages/Insight/widgets.tsx
registryLine: 104
group: インサイト
size: full
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields:
  - insightData
  - insightData.customerData
  - insightData.forecastData
consumedReadModels: []
consumedQueryHandlers: []
children:
  - DecompositionTabContent
lastVerifiedCommit: b23c49a
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-036 — 売上要因分解（Insight）

## 1. 概要

`DecompositionTabContent` に `insightData` のみ forward。`isVisible` + render 内で **複合 predicate**（`customerData != null && forecastData != null`）を 2 箇所で評価（Insight/widgets.tsx:62-72）。

## 2. Data Acquisition Path

- `isVisible: (ctx) => ctx.insightData?.customerData != null && ctx.insightData?.forecastData != null`
- render 内で同等の複合 null check
- 通過時に `<DecompositionTabContent d={ctx.insightData} />`

## 3. Context Fields Consumed — 解説

- 3 field touch（`customerData` / `forecastData` 両 sub-path + `insightData` full object）
- 2 sub-path の両方が必要な合成条件

## 4. Composition

- 直接の子: `DecompositionTabContent`（`./InsightTabForecast` から import、:10）
- inquiry/03 で agent が orphan 誤検出した `InsightTabDecomposition.tsx:33` の export（実際は本 widget で使用中）

## 5. Visibility

- 条件: 2 sub-path の AND

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- 要因分解の不変は `calculateFactorDecomposition` 正本に従属

### Behavior Claims (Phase J Evidence Level)

| ID | claim | evidenceLevel | riskLevel | tests | guards |
|---|---|---|---|---|---|
| CLM-001 | registry 行は view marshalling 専用 — `consumedCtxFields` のみ参照、raw 計算 / fetch 禁止（A3 描画専用 + C9）| reviewed | high | - | - |
| CLM-002 | visibility predicate で空データ時の dead UI 回避（runtime 空状態防御）| reviewed | medium | - | - |
| CLM-003 | DecompositionInsightView wrap で要因分解 Insight、RM-006 Shapley 出力 + CALC-013 discountImpact 統合（widget は orchestration のみ、4 層依存遵守）| guarded | high | - | app/src/test/guards/presentationIsolationGuard.test.ts |

## 7. Co-Change Impact

- `InsightData.customerData` / `forecastData` sub-field 変更
- `DecompositionTabContent` の props shape 変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`
- `factorDecompositionPathGuard`

## 9. Pipeline Concerns / Upstream Requests

- **複合 predicate の widget 層実装**: 2 sub-path の AND を widget 側で表現。該当責務層: application（`InsightData` の completeness state の discriminated union 化検討）
- **二重 null check の 5 例目**
