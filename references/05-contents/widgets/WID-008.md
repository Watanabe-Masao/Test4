---
id: WID-008
kind: widget
widgetDefId: analysis-alert-panel
contextType: Dashboard-local

registry: WIDGETS_EXEC
registrySource: app/src/presentation/pages/Dashboard/widgets/registryExecWidgets.tsx
registryLine: 20

group: モニタリング
size: full
linkTo: null

acquisitionPath: ctx-direct

consumedCtxFields:
  - storeKey
consumedReadModels: []
consumedQueryHandlers: []
children:
  - AlertPanelWidget

lastVerifiedCommit: b23c49a

owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23

specVersion: 1
---

# WID-008 — アラート

## 1. 概要

`AlertPanelWidget` に full ctx を passthrough し、storeKey を React key として指定するアラート表示 registry entry（registryExecWidgets.tsx:20-25）。

## 2. Data Acquisition Path

- registry 行では `<AlertPanelWidget key={ctx.storeKey} ctx={ctx} />` の形で full ctx passthrough + React key 分離

## 3. Context Fields Consumed — 解説

- `storeKey` は React key 用にのみ直接参照（再マウントトリガー）
- 実データ依存は子 `AlertPanelWidget` 内部

## 4. Composition

- 直接の子: `AlertPanelWidget`（`./AlertPanel` から import、:3）

## 5. Visibility

- `isVisible` 未指定（常時可視）

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- アラート判定ロジックは子 `AlertPanelWidget` に閉じる

## 7. Co-Change Impact

- `storeKey: string` field の型変更
- `AlertPanelWidget` の props shape 変更（`ctx: WidgetContext` 想定）

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`

## 9. Pipeline Concerns / Upstream Requests

- **full ctx passthrough の静的解析不能**: 子 `AlertPanelWidget` の実 ctx 依存が不可視
- **`storeKey` を React key として使う widget 群**: WID-008 / WID-014 / WID-015 / WID-016 の 4 widget が同様 pattern（`key={ctx.storeKey}`）。store 切替時の再マウント制御が widget 個別に繰り返される。該当責務層: presentation（key strategy の共通化可否）
