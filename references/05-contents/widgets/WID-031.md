---
id: WID-031
kind: widget
widgetDefId: daily-chart-shapley
contextType: Unified
registry: DAILY_WIDGETS
registrySource: app/src/presentation/pages/Daily/widgets.tsx
registryLine: 33
group: 日別トレンド
size: half
linkTo: null
acquisitionPath: ctx-direct
consumedCtxFields:
  - chartPeriodProps.rangeEnd
  - chartPeriodProps.rangeStart
  - daysInMonth
  - month
  - prevYear.daily
  - prevYear.hasPrevYear
  - result.daily
  - year
consumedReadModels: []
consumedQueryHandlers: []
children:
  - ShapleyTimeSeriesChart
lastVerifiedCommit: f3226c0
owner: implementation
reviewCadenceDays: 90
lastReviewedAt: 2026-04-23
specVersion: 1
---

# WID-031 — シャープリー時系列（Daily）

## 1. 概要

`ShapleyTimeSeriesChart` に 8 field を forward し、`isVisible: (ctx) => ctx.prevYear.hasPrevYear` で前年データ有無を gate する registry entry（Daily/widgets.tsx:29-45）。

## 2. Data Acquisition Path

- 8 field を個別 props で forward
- `prevYear.hasPrevYear ? prevYear.daily : undefined` optional 転送

## 3. Context Fields Consumed — 解説

- 8 field touch
- `isVisible` で `prevYear.hasPrevYear` を gate + render 内で `prevYear.hasPrevYear` を再判定する **二重 check** pattern（WID-040 と同構造）

## 4. Composition

- 直接の子: `ShapleyTimeSeriesChart`（`@/presentation/components/charts`）
- Shapley 分解計算は子側 + domain/calculations/factorDecomposition に従属

## 5. Visibility

- 条件: `(ctx) => ctx.prevYear.hasPrevYear`

## 6. Invariants

- 本 widget 自身は数学的不変条件を持たない
- Shapley 分解は `calculateFactorDecomposition` 正本に従属（`factorDecompositionPathGuard`）

### Behavior Claims (Phase J Evidence Level)

| ID | claim | evidenceLevel | riskLevel | tests | guards | verificationNote |
|---|---|---|---|---|---|---|
| CLM-001 | registry 行は view marshalling 専用 — `consumedCtxFields` のみ参照、raw 計算 / fetch 禁止（A3 描画専用 + C9） | reviewed | high | - | - | 宣言 widget の妥当性は consumedCtxFields 整合 (frontmatterSyncGuard) と R:tag/G8 mechanism (responsibilityTagGuard) で構造的に担保。widget 個別 test は冗長 |
| CLM-002 | visibility predicate で空データ時の dead UI 回避（runtime 空状態防御） | reviewed | medium | - | - | 空状態到達経路は load 中 / fallback / partial 等多岐。widget 個別の predicate test より consumed readModel/calc layer の null 安全契約で構造的保証 |
| CLM-003 | DailyShapleyChart wrap で日次 Shapley 要因分解を描画、RM-006 calculateFactorDecomposition 出力 mapping (Daily ページ)（widget は orchestration のみ、4 層依存遵守） | guarded | high | - | app/src/test/guards/presentationIsolationGuard.test.ts | - |

## 7. Co-Change Impact

- `PrevYearData` の `hasPrevYear` / `daily` 構造変更
- `ShapleyTimeSeriesChart` の props shape 変更
- Shapley 計算の domain 契約変更

## 8. Guard / Rule References

- `responsibilityTagGuard`（R:component）
- `presentationIsolationGuard`
- `factorDecompositionPathGuard`
- `chartInputBuilderGuard`

## 9. Pipeline Concerns / Upstream Requests

- **isVisible + render 内の二重 `hasPrevYear` 判定**: WID-040 の `costDetailData` 二重 null check と同構造（inquiry/04 §D の 17 gate widget の一例）
- **Unified ctx core required `prevYear.hasPrevYear` の gate 用途**: `prevYear` 自身は Unified core required だが `hasPrevYear` で実データ不在を表現する二重 state。該当責務層: domain（`PrevYearData` の discriminated union 化検討）
