/**
 * 後方互換 re-export — 正本は features/forecast/ui/ に移動。
 *
 * @responsibility R:unclassified
 */
export {
  computeWeeklyActuals,
  computeDecompPct,
  computeDecompTotals,
  type DecompTotals,
} from '@/features/forecast'
