/**
 * @responsibility R:unclassified
 */

// Re-export all shared styled components from the styles file
// so that existing imports from './AdminShared' continue to work.
export {
  Section,
  SectionTitle,
  Table,
  Th,
  Td,
  EmptyState,
  Badge,
  StoreIdBadge,
  Select,
  Input,
  HelpText,
} from './AdminShared.styles'
