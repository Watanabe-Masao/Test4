/**
 * 後方互換 re-export — 正本は features/forecast/application/ に移動。
 */
export {
  useForecast,
  useWeekRanges,
  type ForecastInput,
  type ForecastResult,
  type WeeklySummary,
  type DayOfWeekAverage,
} from '@/features/forecast'
